# 🔄 更新日志

### v1.3.0

* ✨ **功能**:
  * 将 `/ol dl`、`/ol ul` 调整为主要用法，同时保留原有下载、上传别名兼容。
  * 增强路径与序号混合操作体验，`/ol info`、`/ol preview`、`/ol ul` 等指令支持按当前列表序号或按路径直接定位目标。
  * 上传模式支持上传到当前目录下的子目录、按序号选择目标目录，并支持引用消息中的文件、图片、视频直接上传。
  * 上传模式补充自动结束机制，开启后 10 分钟未继续操作会自动退出，也可通过指令手动取消。
  * 自动备份启用后会立即执行一次首次全量备份，减少“已开启但尚未落地”的空窗期。
  * 文件预览能力增强，新增 EPUB 文本预览。
* ⚡ **优化**:
  * 重构下载、上传、路径解析、命令分发与运行时配置逻辑，拆分出独立 `lib` 模块，降低主入口复杂度并便于后续维护。
  * 优化路径解析与目录跳转逻辑，统一绝对路径、相对路径、候选路径和当前浏览上下文的处理方式。
  * 新增备份时跳过同名且大小一致文件的能力，减少重复上传并提升自动备份效率。
  * 重组插件配置结构，按连接、显示、缓存、传输、备份、权限、诊断等分组整理 `_conf_schema.json`，配置项层次更清晰。
* 🐛 **修复**:
  * 移除已废弃的公开 `token` 配置项，并兼容清理旧配置中的遗留字段，避免新旧配置混用造成歧义。
  * 修复部分路径补全、文件大小限制、目录选择和下载取链流程中的边界问题，减少因参数格式差异导致的失败。
  * 改善上传、备份、预览等流程的状态反馈与异常提示，提升排查问题时的可读性。

### v1.2.3

* ⚡ **优化**:
  * 将群文件自动备份调整为后台任务，避免短时间连续上传多个文件时导致阻塞。

### v1.2.2

* 🐛 **修复**:
  * 修复了最新框架版本下的兼容性问题。

### v1.2.1

* 🐛 **修复**:
  * 修复了手动备份群文件指令执行时的异常错误。

### v1.2.0

* ✨ **功能**:
  * 新增 `/ol backup` 群文件备份功能。
  * 新增 `/ol autobackup` 自动备份功能。
  * 新增 `/ol restore` 文件恢复功能。
  * 新增 `/ol preview` 文件预览功能。
  * 新增 `/ol rm` 删除文件功能。
  * 新增 `/ol mkdir` 创建目录功能。
* ⚡ **优化**:
  * 新增指令中文别名。

### v1.1.2

* ⚡ **优化**:
  * 简化了分页导航指令，将 `/ol page next` 和 `/ol page prev` 简化为 `/ol next` 和 `/ol prev`。

### v1.1.1

* ⚡ **优化**:
  * 新增了`公网地址`配置项，保证在默认使用内网地址时，能正确获取到可供外部访问的下载链接。

### v1.1.0

* ✨ **功能**:
  * 搜索结果支持分页翻页功能。
  * 支持显示文件夹大小。
* 🐛 **修复**:
  * 修复文件上传的严重错误。
  * 修复完整路径作为指令参数时下载和获取链接失败的问题。

### v1.0.2

* ✨ **功能**:
  * 新增 `/ol page` 分页浏览功能。
  * 明确 `ls` 和 `download` 指令的职责。`ls` 用于浏览和获取链接，`download` 用于直接下载文件。

### v1.0.1

* ⚡ **优化**:
  * 对调了 `ls` 和 `download` 的核心逻辑。现在使用**序号**会获取下载链接，使用**路径**会直接下载文件。
  * 将主指令从 `/openlist` 缩短为 `/ol`，操作更便捷。

### v1.0.0

* ✨ **功能**:
  * 基于 `astrbot_plugin_alistfile` 进行二次开发。
  * 支持基本的文件浏览、搜索、信息查看功能。
  * 支持 OpenList 签名，若服务器端开启签名，插件获取的下载链接可**免登录直接下载**。启用签名可实现免登录下载文件。
